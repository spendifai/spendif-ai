# SpendifAi.SpendifAi.installer.yaml — winget installer manifest
#
# This is a TEMPLATE file. The `PackageVersion`, `InstallerUrl`, and
# `InstallerSha256` fields are updated automatically by packaging/release.sh.
#
# Installer strategy:
#   InstallerType: zip        — the release asset is a ZIP archive
#   NestedInstallerType: script — inside the ZIP, a PowerShell script performs
#                                 the actual installation (clones repo, creates
#                                 venv, creates shortcut). This is the same
#                                 approach as the existing packaging/windows/install.ps1.
#
# The ZIP structure expected by this manifest:
#   zip_staging/
#     install.ps1      ← main installer script
#     create_icon.py   ← Python helper for Windows icon generation
#     README.md
#     VERSION
#     INSTALL.txt      ← quick-start instructions
#
# Schema reference: https://aka.ms/winget-manifest.installer.1.6.0.schema.json

PackageIdentifier: SpendifAi.SpendifAi
PackageVersion: 0.2.0
MinimumOSVersion: "10.0.17763.0"
InstallerType: zip
NestedInstallerType: script
NestedInstallerFiles:
  - RelativeFilePath: zip_staging/install.ps1
    PortableCommandAlias: spendifai-install
InstallModes:
  - interactive
  - silent
Installers:
  - Architecture: x64
    InstallerUrl: https://github.com/spendifai/spendif-ai/releases/download/v0.2.0/SpendifAi-0.2.0-windows.zip
    InstallerSha256: PLACEHOLDER_SHA256_ZIP
    InstallerSwitches:
      Silent: "-NonInteractive"
      SilentWithProgress: "-NonInteractive"
ManifestType: installer
ManifestVersion: 1.6.0
