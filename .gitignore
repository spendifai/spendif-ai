# Python
__pycache__/
.venv/
venv

# Test / coverage artefacts
.coverage
.coverage.*
htmlcov/
.pytest_cache/

# Build artifacts (PyInstaller)
build/
dist/

# Environment / secrets
.env
.env.local
.env.*.local
.env.cloud
secrets/
*.pem
*.key

# Coding agent working dir (diaries, transcripts) — never commit
.coding_agent/

# OS artifacts
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Project
/llm_cache
/backup
/da_cancellare
/logs
tests/logs/
benchmark/logs/
*.csv
# Exception: config CSVs that are part of the codebase
!benchmark/benchmark_models.csv
!benchmark/results_all_runs.csv
# Exception: synthetic test files (needed on bench machines via USB/git)
!benchmark/generated_files/*.csv
*.xls
*.xlsx
!benchmark/generated_files/*.xlsx
*.tmp
my_secrets.py
# Local databases (runtime data, never commit)
*.db
# Schema migration hash (avoids redundant migrations on startup)
.schema_hash
# NSI raw data (large downloaded datasets — not in git)
nsi/


# Local symlink to documents repo (never commit)
rsvd_docs

# .sestante/ — backlog locale, mai committato
.sestante/

.code_reader/
